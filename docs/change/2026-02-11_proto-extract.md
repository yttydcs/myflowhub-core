# 2026-02-11 Proto 拆分 PR1（Core 无实现性变更）

## 背景 / 目标
本轮重构以“协议仓库拆分（Proto）+ Win 上移解耦”为目标，Core 保持稳定：不引入实现性改动，仅用于回归验证与计划同步。

## 具体变更内容（新增 / 修改 / 删除）
### 修改
- `plan.md`
  - 同步本轮跨仓重构的执行计划与范围说明（Core 侧不做实现改动）。

### 新增 / 删除
- 无

## plan.md 任务映射
- C1：回归验证 ✅

## 关键设计决策与权衡（性能 / 扩展性）
- 保持 Core 不动：避免在“多仓依赖拆分”的同批次里叠加 Core 内部重构风险；将改动集中在协议层与 Server/Win 依赖方向收敛。

## 测试与验证方式 / 结果
- `go test ./... -count=1 -p 1`：通过。

## 潜在影响与回滚方案
### 潜在影响
- 无（仅文档/计划更新）。

### 回滚方案
- 回滚对应文档提交即可。

