package core

// 本文件承载 Core 框架中与 `roles` 相关的通用逻辑。

// Metadata keys and values for connection roles.
const (
	MetaRoleKey = "role"

	RoleParent = "parent"
	RoleChild  = "child"
	RoleLocal  = "local"
)
